import React from 'react';
import Layout from './Layout';
import EditedText from './EditedText';

function EditedLetter() {
  return (
    <Layout>
      <EditedText />
    </Layout>
  );
}

export default EditedLetter;
